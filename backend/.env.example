# SmartSeason backend env.
# Leave DATABASE_URL blank to use SQLite automatically (zero setup).
# Or point at Postgres like: postgresql://user:password@localhost:5432/smartseason
DATABASE_URL=
PORT=5000
JWT_SECRET=smartseason-dev-secret-change-me
NODE_ENV=development
